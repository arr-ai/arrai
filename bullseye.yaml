# bullseye.yaml — convergence targets for this project.
#
# Targets are desired project states expressed as testable properties,
# with dependency tracking and frontier computation. This file is the
# source of truth: read and written by the bullseye MCP server.
#
# AGENT RULE: Do NOT edit this file directly unless the user explicitly
# instructs you to, or the file is unloadable and you are performing an
# emergency repair. Prefer Bullseye MCP/CLI mutation tools (open/query/
# commit). Direct edits are detected via content_hash below.
#
# What is bullseye / how do I install it?
#   https://github.com/marcelocantos/bullseye
#
# This banner is regenerated by bullseye on every write — edit targets
# only through tools, not these lines.
# content_hash: sha256:3698f46d98d66059502b9a34243f89cab24f45dc58c1a382f581205daec2dc1d

schema_version: 5
targets:
  T1:
    name: 'Frozen read-path allocations are zero  [high, weight: 9]'
    status: achieved
    value: 1.0
    cost: 1.0
    acceptance:
    - TODO
    context: 'Achieved 2026-03-07. Merged to frozen master as PR #88 (5b2842e). All benchmarks at 0 allocs for concrete key types, 1 alloc for interface keys.'
    origin: manual
    discovered: 2026-04-09
    achieved: 2026-04-11
  T2:
    name: 'Frozen write-path allocations minimised  [medium, weight: 5]'
    status: achieved
    value: 1.0
    cost: 1.0
    acceptance:
    - TODO
    context: Retired 2026-03-08. Benchmarking showed write-path allocations (3-18 for Map.With, 1-7 for Set.With) are structurally inherent to persistent data structures (spine-copying). Already reasonable for tree depth. Read-path fix (🎯T1) addressed the actual regression. No concrete workload shows write-path as a bottleneck.
    origin: manual
    discovered: 2026-04-09
    achieved: 2026-04-11
  T3:
    name: 'arrai v0.333.0 released  [high, weight: 8]'
    status: achieved
    value: 1.0
    cost: 1.0
    acceptance:
    - TODO
    context: Achieved 2026-03-08. Release created automatically by generate-tag workflow. Tag `v0.333.0` on master, GitHub release with binaries for all platforms.
    origin: manual
    discovered: 2026-04-09
    achieved: 2026-04-11
  T4:
    name: 'Agent guide is discoverable from the repo root  [medium, weight: 5]'
    status: achieved
    value: 1.0
    cost: 1.0
    acceptance:
    - TODO
    context: Achieved 2026-03-08. Root symlink `agents-guide.md` → `cmd/arrai/agents-guide.md`, README updated with agent guide section.
    origin: manual
    discovered: 2026-04-09
    achieved: 2026-04-11
  T5:
    name: docs/ npm audit is clean
    status: achieved
    value: 3.0
    cost: 3.0
    acceptance:
    - '`cd docs && npm audit` reports 0 vulnerabilities'
    - '`cd docs && npm run build` succeeds with no new errors'
    - No runtime regressions in the built docs site
    context: |-
      Achieved 2026-07-15. Surgical npm `overrides` in docs/package.json pin vulnerable
      transitive leaves (serialize-javascript, picomatch, brace-expansion, path-to-regexp,
      webpack-dev-server, ws, shell-quote, js-yaml@3.15, etc.) without advancing webpack
      past webpackbar compatibility. `npm audit` → 0; `npm run build` succeeds.
    tags:
    - security
    - docs
    - dependencies
    origin: Surfaced 2026-04-11 while fixing lodash CVE GHSA-r5fr-rjxr-66jc (commit 0b34341). `npm audit` flagged 22 remaining advisories; triage showed they're all stale transitive pins, not genuinely unfixable.
    discovered: 2026-04-11
    achieved: 2026-07-15
  T6:
    name: Netlify docs deploy preview builds cleanly
    status: achieved
    value: 2.0
    cost: 2.0
    acceptance:
    - Netlify deploy preview succeeds on docs PRs (deploy/netlify, Header rules, Pages changed, Redirect rules all pass)
    - No manual dashboard config drift — build config lives in repo (netlify.toml or equivalent)
    context: |-
      Achieved 2026-07-15. Added root netlify.toml with base=docs, command=`npm ci && npm run build`,
      publish=build so Netlify uses the committed package-lock.json and npm overrides instead of
      yarn (which ignored overrides and broke webpackbar).
    tags:
    - docs
    - ci
    - netlify
    origin: Surfaced 2026-04-11 while releasing v0.336.0 via /release. GitHub Actions docs workflow was broken identically and got fixed; Netlify pipeline is separate and still broken.
    discovered: 2026-04-11
    achieved: 2026-07-15
  T7:
    name: 'Variable lookup never hashes: scopes are lexical frames and identifier reads resolve to a frame slot'
    status: identified
    value: 0.0
    cost: 0.0
    acceptance:
    - rel.Scope is backed by chained frames (parent pointer + small name/value slices), not a frozen.Map; With/Update allocate one frame, no hashing
    - IdentExpr reads resolve to (depth, slot) after first evaluation and verify the slot on hit, falling back to a name walk; correctness never depends on the cached address
    - go test ./... (incl. -race -tags timingsensitive) and ./arrai test pass; reconstruct repro output byte-identical to v0.321.0
    - Scope.Update, frozen Map.Combine and hash.String on identifier names no longer appear in the repro's top-100 CPU nodes
    - Repro wall time improves measurably over v0.339.0 (4.4–5.1s) and the number is recorded in the commit message
    context: After v0.339.0, scope merges (Scope.Update → HAMT Combine, per-op EqArgs/CombineArgs, branch/leaf copies) are ~6.5% CPU and ~30% of remaining allocation, and every identifier read hashes its name. Scoping is lexical, so frames have static shape and reads can be addressed by slot. Full closure conversion (compile-time-only resolution, flat upvalue arrays) is the follow-on if the frame walk for module-level names still shows in profiles.
    tags:
    - performance
    - evaluator
    origin: manual
    discovered: 2026-08-29
