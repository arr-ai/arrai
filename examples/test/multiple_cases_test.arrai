# 5 test cases

let testCase1 = (
    let words = ["Hello", "arrai!"];
    //test.assert.equal("Hello, arrai!", //seq.join(", ", words))
);

let testCase2 = (
    let words = ["Nihao", "arrai!"];
    //test.assert.unequal("Hello, arrai!", //seq.join(", ", words))
);

let testCase3 = (
    let words = {"Hello", "arrai!"};
    //test.assert.size(2, words)
);

let testCase4 = (
    //test.assert.true({"Hello", "arrai!"})
);

let testCase5 = (
    //test.assert.false({})
);

let testCases = (:testCase1, :testCase2, :testCase3, :testCase4, :testCase5);
testCases
